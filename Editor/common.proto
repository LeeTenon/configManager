// 此文件存放通用配置区结构体
syntax = "proto3";

package config;

import "types.proto";

message RpcServerConfig {
  Etcd etcd = 1;
  Database database = 2;
  TaskHandler taskHandler = 3;
  int64 times=4;
  Mode mode = 10;
}